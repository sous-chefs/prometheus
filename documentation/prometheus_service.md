# prometheus_service

Manages the Prometheus systemd unit.

## Actions

* `:create`
* `:enable`
* `:start`
* `:restart`
* `:reload`
* `:stop`
* `:delete`

## Properties

Common properties include `binary`, `version`, `flags`, `cli_options`, `cli_flags`, `user`, and `group`.
