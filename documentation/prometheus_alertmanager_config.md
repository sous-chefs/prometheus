# prometheus_alertmanager_config

Writes the Alertmanager configuration file.

## Actions

* `:create`
* `:delete`

## Properties

Common properties include `config_file`, `template_cookbook`, `template_source`, `notification_config`, `user`, and `group`.
