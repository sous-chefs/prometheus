---
driver:
  name: vagrant

transport:
  max_wait_until_ready: 600
  connection_retries: 10
  connection_retry_sleep: 10

provisioner:
  name: chef_infra
  product_name: chef
  product_version: <%= ENV['CHEF_VERSION'] || 'latest' %>
  channel: stable
  install_strategy: once
  chef_license: accept
  deprecations_as_errors: true

verifier:
  name: inspec
  sudo: true

platforms:
  - name: ubuntu-24.04
  - name: debian-12
  - name: almalinux-9

x-run_lists:
  default: &default_run_list
    - recipe[test::default]
  alertmanager: &alertmanager_run_list
    - recipe[test::alertmanager]

x-verifiers:
  default: &default_verifier
    inspec_tests:
      - path: test/integration/default
  alertmanager: &alertmanager_verifier
    inspec_tests:
      - path: test/integration/alertmanager

suites:
  - name: default
    run_list: *default_run_list
    verifier: *default_verifier
  - name: alertmanager
    run_list: *alertmanager_run_list
    verifier: *alertmanager_verifier
